import React from "react";
import ReactDOM from "react-dom/client";
import { HealthTrackApp } from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <HealthTrackApp />
  </React.StrictMode>
);
